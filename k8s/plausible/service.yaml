apiVersion: v1
kind: Service
metadata:
  name: plausible
  namespace: plausible
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: plausible
